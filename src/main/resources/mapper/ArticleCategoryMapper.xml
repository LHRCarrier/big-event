<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bubbles.server.mapper.ArticleCategoryMapper">
    <insert id="add">
        insert into category(category_name, category_alias, create_user, create_time, update_time)
        VALUES (#{categoryName},#{categoryAlias},#{createUser},#{createTime},#{updateTime})
    </insert>
    <select id="pageQuery" resultType="com.bubbles.pojo.vo.ArticleCategoryVO">
        select category.id,category.category_name,category.category_alias from category
        <where>
            <if test="categoryName != null and categoryName !='' ">AND category_name = #{categoryName}</if>
            <if test="categoryAlias != null and categoryAlias !='' ">AND category_alias = #{categoryAlias}</if>
            <if test="createTime != null ">AND create_time = #{createTime}</if>
            <if test="updateTime != null ">AND update_time = #{updateTime}</if>
        </where>
        order by create_time desc
    </select>
    <update id="update">
        update category
        <set>
            <if test="categoryName !=null"> category_name = #{categoryName},</if>
            <if test="categoryAlias !=null"> category_alias = #{categoryAlias},</if>
            <if test="updateTime !=null "> update_time = #{updateTime}</if>
        </set>
        where id = #{id}
    </update>
</mapper>

